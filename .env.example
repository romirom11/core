VERSION=0.7.18

# Nest run in docker, change host to database container name
DB_HOST=localhost
DB_PORT=5432

# POSTGRES
POSTGRES_USER=docker
POSTGRES_PASSWORD=docker
POSTGRES_DB=core

LOGIN_ORIGIN=http://localhost:3033
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@postgres:5432/${POSTGRES_DB}?schema=core

# This sets the URL used for direct connections to the database and should only be needed in limited circumstances
# See: https://www.prisma.io/docs/reference/api-reference/prisma-schema-reference#fields:~:text=the%20shadow%20database.-,directUrl,-No
DIRECT_URL=${DATABASE_URL}

REMIX_APP_PORT=3033
APP_ENV=production
NODE_ENV=${APP_ENV}
APP_ORIGIN=http://localhost:3033
API_BASE_URL=${APP_ORIGIN}
# Background jobs call the app's own API through this URL. Leave blank to use
# APP_ORIGIN. Set it (e.g. http://localhost:3000) when the public origin is
# unreachable from inside the container — tailnet HTTPS, firewalled loopback —
# or every scheduled task dies with "fetch failed".
INTERNAL_API_URL=


SESSION_SECRET=27192e6432564f4788d55c15131bd5ac
ENCRYPTION_KEY=27192e6432564f4788d55c15131bd5ac

########### Sign.in with google ############
AUTH_GOOGLE_CLIENT_ID=
AUTH_GOOGLE_CLIENT_SECRET=

REDIS_HOST=redis
REDIS_PORT=6379
REDIS_TLS_DISABLED=true

ENABLE_EMAIL_LOGIN=true

NEO4J_URI=bolt://neo4j:7687
NEO4J_USERNAME=neo4j
NEO4J_PASSWORD=27192e6432564f4788d55c15131bd5ac
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_API_MODE=responses
ANTHROPIC_API_KEY=
ANTHROPIC_BASE_URL=
GOOGLE_GENERATIVE_AI_API_KEY=
GEMINI_BASE_URL=

# Each *_BASE_URL is optional. Leave blank to reach the provider directly.
# Point them at a gateway (e.g. CLIProxyAPI) to route each model family over its
# own native protocol. Every SDK appends its own path to the base URL, so each
# variable carries the prefix up to (but not including) that path:
#   OPENAI_BASE_URL     -> .../v1       + /chat/completions
#   ANTHROPIC_BASE_URL  -> .../v1       + /messages
#   GEMINI_BASE_URL     -> .../v1beta   + /models/{model}:generateContent
# For a gateway on http://gateway:8317 that means:
#   OPENAI_BASE_URL=http://gateway:8317/v1
#   ANTHROPIC_BASE_URL=http://gateway:8317/v1
#   GEMINI_BASE_URL=http://gateway:8317/v1beta

MAGIC_LINK_SECRET=27192e6432564f4788d55c15131bd5ac
NEO4J_AUTH=neo4j/27192e6432564f4788d55c15131bd5ac
OLLAMA_URL=http://ollama:11434

EMBEDDING_MODEL=text-embedding-3-small
EMBEDDING_MODEL_SIZE=1536
MODEL=gpt-5.2-2025-12-11

# Provider routing (optional)
CHAT_PROVIDER=openai
# EMBEDDINGS_PROVIDER=openai
# Local Ollama: set CHAT_PROVIDER=ollama, OLLAMA_URL=http://localhost:11434 (bare host —
# no /api or /v1 suffix, the app appends them), MODEL=<pulled model>, EMBEDDINGS_PROVIDER=ollama,
# EMBEDDING_MODEL_SIZE = the model's real dim (nomic-embed-text=768)
# Override tolerant JSON parsing behavior (optional). Controls error tolerance when parsing LLM responses
# Set to 'true' to allow lenient parsing, or leave empty for strict mode: true/1/yes or false/0/no
LLM_TOLERANT_OUTPUT=

########### Reranking Configuration ############
# Choose reranking provider: 'cohere', 'ollama', or 'none'
# - 'cohere': Use Cohere API for high-quality reranking (requires COHERE_API_KEY)
# - 'ollama': Use local Ollama model for reranking (requires OLLAMA_URL and a rerank model)
# - 'none': No reranking, use original search algorithm scores
RERANK_PROVIDER=none

# Cohere settings (when RERANK_PROVIDER=cohere)
COHERE_API_KEY=
COHERE_RERANK_MODEL=rerank-english-v3.0
COHERE_SCORE_THRESHOLD=0.3

# Ollama settings (when RERANK_PROVIDER=ollama)
# Model examples: qwen2.5:14b-rerank, bge-reranker-v2-m3
OLLAMA_RERANK_MODEL=dengcao/Qwen3-Reranker-8B:Q5_K_M
OLLAMA_SCORE_THRESHOLD=0.3

## AWS Bedrock ##
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_REGION=us-east-1

QUEUE_PROVIDER=bullmq

# WhatsApp (Twilio)
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=

# Email (Resend)
# EMAIL_TRANSPORT=resend
# RESEND_API_KEY=
FROM_EMAIL=
